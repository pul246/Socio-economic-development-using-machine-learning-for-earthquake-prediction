In your vscode, set up the flask virtual environment:
- In the terminal enter the commands:
   python -m venv venv
   venv\Scripts\activate
   pip install flask

Then run the application by the command:
   flask run
